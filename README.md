This react-admin framework demo code was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Getting started

Follow these steps:

> Install dependencies: `yarn install`

> Start a project: `yarn start`

This project should be up and running with an empty React application on port 3000.

